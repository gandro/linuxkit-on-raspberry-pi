image: chrony
org: linuxkitrpi
network: true
config:
  binds:
   - /etc/resolv.conf:/etc/resolv.conf
   - /var/lib/chrony:/var/lib/chrony
  tmpfs:
   - /var/run/chrony
  runtime:
    mkdir: ["/var/lib/chrony"]
  capabilities:
   - CAP_SYS_TIME
